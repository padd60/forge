import { PlaceOrderForm } from '@/features/place-order';

export function Checkout() {
  return (
    <section>
      <h2>Checkout</h2>
      <p>Place your order — the use case lives in features/.</p>
      <PlaceOrderForm />
    </section>
  );
}
